import setuptools

setuptools.setup(
    name="weakdb",
    version="0.0.0",
    url="",
    description="Debugging weak supervision sources.",
    install_requires=[],  # TODO: put reqs here
    packages=['weakdb'],
)
